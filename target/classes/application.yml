server:  port: 8761 #指定服务端口  application:    name: movie-service #注册到Eureka Server上的应用名eureka:  client:    registerWithEureka: false #默认true，是否将eureka自身作为应用注册到eureka注册中心    fetchRegistry: false #默认true,是否从Eureka Server获取注册信息，为true时，可以启动，但报异常：Cannot execute request on any known server    serviceUrl:      defaultZone: http://localhost:8761/eureka/ #设置与Eureka Server交互的地址，查询服务和注册服务都需要依赖这个地址。                                                 #默认地址：http://localhost:8761/eureka/ ；多个地址可以使用,分隔